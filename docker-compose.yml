services:
  bot:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
      - ~/.claude.json:/root/.claude.json:ro
    restart: unless-stopped
